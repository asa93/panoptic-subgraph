{
  "name": "panopticsubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ panopticsubgraph",
    "create-local": "graph create --node http://localhost:8020/ panopticsubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ panopticsubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 panopticsubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "^0.27.0",
    "assemblyscript": "0.21.3"
  }
}
