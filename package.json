{
  "name": "panoptictest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ panoptictest",
    "create-local": "graph create --node http://localhost:8020/ panoptictest",
    "remove-local": "graph remove --node http://localhost:8020/ panoptictest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 panoptictest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
